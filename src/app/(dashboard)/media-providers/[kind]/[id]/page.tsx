import MediaProviderDetailClient from "./MediaProviderDetailClient";

export const metadata = { title: "Media Providers" };

export default function MediaProviderDetailPage() {
  return <MediaProviderDetailClient />;
}
