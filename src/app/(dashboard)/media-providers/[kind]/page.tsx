import MediaProviderKindClient from "./MediaProviderKindClient";

export const metadata = { title: "Media Providers" };

export default function MediaProviderKindPage() {
  return <MediaProviderKindClient />;
}
