import MediaProviderComboClient from "./MediaProviderComboClient";

export const metadata = { title: "Media Providers" };

export default function MediaProviderComboPage() {
  return <MediaProviderComboClient />;
}
