# RWX Run Configuration - Format & Lint Check
# biome format + biome lint + oxlint
# Usage: rwx run .rwx/format.yml --wait

on:
  cli:
    init:
      commit-sha: ${{ event.git.sha }}

base:
  image: ubuntu:24.04
  config: rwx/base 1.0.2

tasks:
  - key: code
    call: git/clone 2.0.7
    with:
      repository: https://github.com/lazuardytech/pod.git
      ref: ${{ init.commit-sha }}
      github-token: ${{ github.token }}

  - key: bun
    call: nodejs/install 1.1.14
    with:
      node-version: 22.14.0

  - key: bun-install
    use: [code, bun]
    run: |
      curl -fsSL https://bun.sh/install | bash
      export PATH="$HOME/.bun/bin:$PATH"
      bun install --frozen-lockfile
    filter:
      - package.json
      - bun.lock

  - key: run-check
    use: [bun-install]
    run: |
      export PATH="$HOME/.bun/bin:$PATH"
      bun run check
    timeout: 5m

  - key: results
    use: [run-check]
    run: |
      echo "================================================"
      echo "  FORMAT + LINT CHECK COMPLETE"
      echo "  biome format + biome lint + oxlint: OK"
      echo "================================================"
